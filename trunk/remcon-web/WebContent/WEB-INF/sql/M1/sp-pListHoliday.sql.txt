DROP PROCEDURE IF EXISTS pListHoliday;
DELIMITER $$

CREATE PROCEDURE pListHoliday(IN vEmployee BIGINT)
BEGIN
	SELECT		cId, cFrom, cNormal, cProgressive, fBusinessDate(cFrom, cNormal + cProgressive) AS cTo
	FROM 		tHoliday
	WHERE		cEmployee = vEmployee;

END$$

DELIMITER ;

