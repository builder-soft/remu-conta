DROP PROCEDURE if exists remcon.pListAPV;
DROP PROCEDURE if exists remcon.pGetAPVList;
DELIMITER $$

CREATE PROCEDURE remcon.pListAPV()
BEGIN
	SELECT		cId, cKey, cName 
	FROM 		remcon.tAPV;
END$$

DELIMITER ;
 
