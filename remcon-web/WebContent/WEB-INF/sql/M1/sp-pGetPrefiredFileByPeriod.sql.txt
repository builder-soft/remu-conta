DROP PROCEDURE if exists remcon.pGetPrefiredFileByPeriod;
DELIMITER $$

CREATE PROCEDURE remcon.pGetPrefiredFileByPeriod(IN pEmployee BIGINT)
BEGIN
	SELECT		cId 
	FROM 		remcon.tAgreement
	WHERE		cEmployee = pEmployee;

END$$

DELIMITER ;

